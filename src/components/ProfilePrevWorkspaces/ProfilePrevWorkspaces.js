import React from "react";
import classes from "./ProfilePrevWorkspaces.module.css";

const ProfilePrevWorkspaces = (props) => {
  return <div className={classes.mainAreaContainer}>No Previous workspaces were found.</div>;
};

export default ProfilePrevWorkspaces;
